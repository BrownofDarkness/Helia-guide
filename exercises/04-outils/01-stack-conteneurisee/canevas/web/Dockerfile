# Dockerfile web — à compléter

FROM node:22-alpine AS dev
WORKDIR /app

# TODO : copier package*.json et installer
# TODO : créer un user non-root
EXPOSE 5173

# TODO : commande de démarrage Vite
# Indice : npm run dev -- --host 0.0.0.0
