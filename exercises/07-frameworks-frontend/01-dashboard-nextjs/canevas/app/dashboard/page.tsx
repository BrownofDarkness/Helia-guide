import { getStats } from '@/lib/fake-data';

export default async function DashboardPage() {
  // TODO 7 : appeler getStats() et afficher les 3 stats dans des cards

  return (
    <div>
      <h1 className="text-3xl font-bold mb-6">Tableau de bord</h1>
      <p>TODO : afficher les stats (tâches totales, faites, en attente)</p>
    </div>
  );
}
